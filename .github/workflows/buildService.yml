name: Build Service

on:
  workflow_dispatch:
  pull_request:
    paths-ignore: ['*.md']
    branches: ['main', 'master', 'update/040', 'update/040-new']
  push:
    paths-ignore: ['*.md']
    branches: ['main', 'master', 'update/040', 'update/040-new']

jobs:
  build:
    uses: start9labs/shared-workflows/.github/workflows/buildService.yml@master
    # with:
    #   FREE_DISK_SPACE: true
    secrets:
      DEV_KEY: ${{ secrets.DEV_KEY }}